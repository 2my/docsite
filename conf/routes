# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET /                           controllers.Application.index
GET /login                      controllers.Application.login
GET /login/auth		controllers.Application.auth()
GET /login/verify	controllers.Application.verify()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# fall-through tries to render markdown
GET     /*file.html               	      controllers.MarkDown.html( file )
GET     /*file.md               	      controllers.MarkDown.md( file )
GET     /*file               	      	  controllers.MarkDown.page( file )
